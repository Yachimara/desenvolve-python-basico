#Entrada de dados
n1=float(input("Digite o primeiro número: "))
n2=float(input("Digite o segundo número: "))

#Processamento

r=round(abs(n1-n2),2)

#Impressão
print("A diferença absoluta entre os números é:", r)
